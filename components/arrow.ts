// U+2197 followed by VS15 (U+FE0E). The variation selector forces text
// presentation — without it iOS falls back to Apple Color Emoji and the
// arrow renders as a blue emoji instead of inheriting the surrounding colour.
export const ARROW = "↗︎";
